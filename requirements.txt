# Core agent framework
langgraph>=0.4.0
langchain>=0.3.0
langchain-google-genai>=2.0.0

# LangGraph PostgreSQL checkpointing
langgraph-checkpoint-postgres>=2.0.0
psycopg[binary]>=3.1.0

# Google GenAI SDK
google-genai>=0.2.0

# API layer
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9    # required for UploadFile
python-dotenv>=1.0.0       # required for loading .env files locally
PyJWT>=2.8.0               # required for JWT authentication
passlib[bcrypt]>=1.7.4     # required for password hashing

# Database
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0     # SQLAlchemy adapter (distinct from psycopg3 above)

# Document processing
pdfplumber>=0.11.0          # PDF text extraction
python-docx>=1.1.0          # DOCX extraction
pillow>=10.0.0              # Image handling
pytesseract>=0.3.10         # OCR (requires Tesseract system install)
pymupdf>=1.24.0             # PDF→image for scanned PDFs

# Validation
pydantic>=2.7.0

# Observability (optional but recommended)
langsmith>=0.1.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0               # for FastAPI TestClient
